import stylex from "@stylexjs/stylex";

const colors = stylex.create({
  red: {
    color: "red",
  },
  blue: {
    color: "blue",
  },
  green: {
    color: "green",
  },
});

export default colors;
